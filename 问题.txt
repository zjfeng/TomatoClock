1 {
	多个任务同时存在，开始非第一个任务，任务结束，完成数不会改变。
}(解决)

2 {
	多个任务同时存在，开始第一个任务，任务结束，第二个任务的完成数改变。改变的值是第一个任务的完成数需要改变的值。
}（解决）

3 {
	实现：排序非第一的任务不可开始。
}(解决)

4 {
	同一个任务无法进行多次，除第一次点击开始后显示正确时间外，其他次数显示第一次执行完成后的时间，且不经过计时完成次数直接加一。
}(解决)